CREATE TABLE TEST1 (
    ID VARCHAR(36) NOT NULL,
    CREATED_DATE ${datetime},
    MODIFIED_DATE ${datetime},
    PRIMARY KEY (ID)
);

-- comment
-- comment 2

CREATE TABLE TEST2 (
    ID VARCHAR(36) NOT NULL,
    CREATED_DATE ${datetime},
    MODIFIED_DATE ${datetime},
    PRIMARY KEY (ID)
);